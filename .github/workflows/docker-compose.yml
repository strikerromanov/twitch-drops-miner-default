name: Deploy via Docker Compose

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy to server
        run: |
          echo "Add your server deployment steps here"
          echo "Example: SSH into your server and run:"
          echo "  docker compose pull"
          echo "  docker compose up -d"
